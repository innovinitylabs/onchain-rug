# 🧠 Standalone x402 Rug Maintenance AI Agent Configuration

# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Blockchain Configuration (Base Sepolia - Direct Payment)
RPC_URL=https://sepolia.base.org
CHAIN_ID=84532
CONTRACT_ADDRESS=0x3d6670aC0A881Dcc742c17D687F5dfE05Af81cff

# Agent Wallet Configuration (for real transactions)
# IMPORTANT: Set BOTH private key and address
# This is the wallet that signs X402 payments and executes blockchain transactions
AGENT_PRIVATE_KEY=0x_your_64_character_private_key_here
AGENT_ADDRESS=0x_your_40_character_wallet_address_here

# ⚠️  IMPORTANT: Authorize this agent first!
# Before running the agent, rug owners must authorize it via dashboard:
# 1. Go to /dashboard in your web app
# 2. Find "AI Agent Authorization" section
# 3. Enter this agent's address (AGENT_ADDRESS above)
# 4. Click "Authorize Agent" and confirm in MetaMask
# 5. Only THEN can the agent maintain rugs for that owner

# Test Configuration
TEST_TOKEN_ID=1
AUTO_MAINTAIN=false
MAINTENANCE_CHECK_INTERVAL=300000

# x402 Payment Configuration (Custom Facilitator)
# Uses our built-in X402 facilitator on Base Sepolia
X402_FACILITATOR_URL=http://localhost:3000/api/x402/facilitator
X402_PAY_TO_ADDRESS=0x_your_merchant_address_here

# Website API URL (where blockchain transactions are executed)
WEBSITE_API_URL=https://onchainrugsworking-j4mjphr80-valipokkanns-projects.vercel.app

# Note: No API key needed for custom facilitator

# Agent Personality (optional)
AGENT_NAME=RugBot
AGENT_STYLE=helpful,professional,enthusiastic
